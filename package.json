{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "lerna": "^2.11.0"
    },
    "scripts": {
        "clean": "leran run clean",
        "build": "lerna run build",
        "test": "lerna run test",
        "publish": "lerna run clean & lerna run build & lerna run test & lerna publish"
    }
}
