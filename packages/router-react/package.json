{
	"name": "@twii/router-react",
	"description": "twii router react lib",
	"version": "3.0.1",
	"license": "MIT",
	"main": "lib/index.js",
	"dependencies": {
		"@twii/router": "^3.0.1",
		"react": "^16.6.3",
		"react-dom": "^16.6.3"
	},
	"devDependencies": {
		"@types/jest": "^21.1.5",
		"del-cli": "^1.1.0",
		"identity-obj-proxy": "^3.0.0",
		"jest": "^24.1.0",
		"ts-jest": "^24.0.0",
		"typescript": "^3.0.3"
	},
	"scripts": {
		"clean": "del-cli lib --force && del-cli lib-commonjs --force",
		"build": "tsc && tsc -p tsconfig.cjs.json",
		"build-watch": "tsc --watch",
		"test": "jest"
	}
}
